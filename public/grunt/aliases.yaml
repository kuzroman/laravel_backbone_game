default:
 description: 'Сборка по умолчанию'
 tasks:
  - 'browserSync'
  - 'webpack'
#  - 'watch' # теперь использую вотчер от weppack!

build:
 description: 'Сборка в продакшен'
 tasks:
  - 'webpack'
